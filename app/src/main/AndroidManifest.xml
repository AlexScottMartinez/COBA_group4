<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coba_group4">

    <uses-permission-sdk-23 android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.COBA_group4">
        <activity android:name=".occurence.OccurrenceData"/>
        <activity android:name=".Map"/>
        <activity android:name=".ForumMessaging" />
        <activity android:name=".Forum" />
        <activity android:name=".occurence.ReportOccurrence" />
        <activity android:name=".Search" />
        <activity android:name=".Emergency" />
        <activity android:name=".ProfilePage" />
        <activity android:name=".SignUp" />
        <activity android:name=".Login"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:theme="@style/Theme.Design.NoActionBar">
        </activity>
    </application>

</manifest>